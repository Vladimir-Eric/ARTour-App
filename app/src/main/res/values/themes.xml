<!-- values\themes.xml -->
<resources>

    <!-- Base application theme. -->
    <style name="Base.Theme.ARTour" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="fragment_pozadina">@color/ghost_white</item>
        <item name="naslovna_linija">@color/primarna</item>
        <item name="kartica">@color/white</item>
        <item name="heading_text">@color/primarna</item>
        <item name="kategorije_ikonice">@color/ghost_white</item>
        <item name="slicice">@color/primarna_tekst</item>
        <item name="kartica_nova">@color/white</item>
        <item name="theme_text">@color/black</item>

        <!-- Optional: set to transparent if your app is drawing behind the status bar. -->
        <item name="android:statusBarColor">@color/primarna</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Ikone statusne linije da budu svijetle (bijele) -->
        <item name="android:windowLightStatusBar">false</item>

    </style>

    <style name="BottomNavStyle" parent="Widget.Design.BottomNavigationView">
        <item name="fontFamily">@font/carossoftextrabold</item>
        <item name="iconTint">@color/white</item>
        <item name="itemTextColor">#148c94</item>
        <!-- Dodajte ostale stilove prema vašim potrebama -->
    </style>

    <style name="Theme.ARTour" parent="Base.Theme.ARTour">
        <!-- Transparent system bars for edge-to-edge. -->
        <!--<item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>-->
    </style>

</resources>